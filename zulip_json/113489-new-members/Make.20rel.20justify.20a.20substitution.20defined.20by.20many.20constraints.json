[
    {
        "content": "<p>I wrote a proof from <em>The Mechanics..</em>  (<code>calc + add_le_add_left-add_le_add_right</code> or <code>linarith</code>), but I hoped I can solve it using <code>rel</code> or something similar, given <code>H : a ≤ 1 ∧ a + b ≤ 3</code> and <code>LHS = a + (a + b)</code>. <code>Rel</code>, however, was not able to justify a substitution.</p>\n<div class=\"spoiler-block\"><div class=\"spoiler-header\">\n<p>Proof with calc + add_le_add_left-add_le_add_right</p>\n</div><div class=\"spoiler-content\" aria-hidden=\"true\">\n<div class=\"codehilite\"><pre><span></span><code>import Mathlib\nimport Lean\n\nexample {a b : ℚ} (H : a ≤ 1 ∧ a + b ≤ 3) : 2 * a + b ≤ 4 := by\n  calc\n    2 * a + b = a + (a + b) := by ring\n    _ ≤ a + 3 := add_le_add_left H.2 a\n    _ ≤ 1 + 3 := add_le_add_right H.1 3\n    _ ≤ 4 := by norm_num\n</code></pre></div>\n</div></div>\n<div class=\"spoiler-block\"><div class=\"spoiler-header\">\n<p>Proof with linarith</p>\n</div><div class=\"spoiler-content\" aria-hidden=\"true\">\n<div class=\"codehilite\"><pre><span></span><code>import Mathlib\nimport Lean\n\nexample {a b : ℚ} (H : a ≤ 1 ∧ a + b ≤ 3) : 2 * a + b ≤ 4 := by\n  linarith\n</code></pre></div>\n</div></div>",
        "id": 483051797,
        "sender_full_name": "Michael Bucko",
        "timestamp": 1731943627
    },
    {
        "content": "<p>I guess I have to look at the actual code of <code>linarith</code>/ <code>nlinarith</code>. They are solving a bunch of other proofs from <em>The Mechanics</em> . </p>\n<div class=\"spoiler-block\"><div class=\"spoiler-header\">\n<p>More proofs</p>\n</div><div class=\"spoiler-content\" aria-hidden=\"true\">\n<div class=\"codehilite\"><pre><span></span><code>import Mathlib\nimport Lean\n\nexample {a b : ℚ} (H : a ≤ 1 ∧ a + b ≤ 3) : 2 * a + b ≤ 4 := by\n  linarith\n\nexample {r s : ℝ} (H : r + s ≤ 1 ∧ r - s ≤ 5) : 2 * r ≤ 6 := by\n  linarith\n\nexample {m n : ℤ} (H : n ≤ 8 ∧ m + 5 ≤ n) : m ≤ 3 := by\n  linarith\n\nexample {p : ℤ} (hp : p + 2 ≥ 9) : p ^ 2 ≥ 49 ∧ 7 ≤ p := by\n  constructor\n  . nlinarith\n  . linarith\n\nexample {a : ℚ} (h : a - 1 ≥ 5) : a ≥ 6 ∧ 3 * a ≥ 10 := by\n  constructor\n  . linarith\n  . linarith\n\nexample {x y : ℚ} (h : x + y = 5 ∧ x + 2 * y = 7) : x = 3 ∧ y = 2 := by\n  constructor\n  . linarith\n  . linarith\n</code></pre></div>\n</div></div>\n<p>, and so on.</p>",
        "id": 483057014,
        "sender_full_name": "Michael Bucko",
        "timestamp": 1731944784
    }
]