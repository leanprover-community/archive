[
    {
        "content": "<p>Previously we've been bumping versions manually, and they kept getting out of sync with lean-toolchain.</p>\n<p>This also adds a regular CI step that only runs when <code>lean-toolchain</code> is touched by the PR, and that checks that lean4checker is available for this version, but doesn't actually run it (this still happens in cron).</p>",
        "id": 481018655,
        "sender_full_name": "Kim Morrison",
        "timestamp": 1730936581
    }
]