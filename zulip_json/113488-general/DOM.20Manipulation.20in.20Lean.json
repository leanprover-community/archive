[
    {
        "content": "<p>Hello, relatively new to Lean here.</p>\n<p>What options are there for manipulating the DOM from Lean? I imagine I would need a model of the DOM, then an Elm-like model-view-update loop, then a way to call JavaScript to do this manipulation.</p>\n<p>What's the state of the art? Who's working on this?</p>\n<p>I think it might be possible to compile Lean to WASM, but I'm not sure.</p>\n<p>I found several projects that roughly do this, but they all look very early stages.<br>\n<a href=\"https://github.com/theoriclabs/lean-react\">https://github.com/theoriclabs/lean-react</a><br>\n<a href=\"https://github.com/ejgallego/lean-vir\">https://github.com/ejgallego/lean-vir</a><br>\n<a href=\"https://github.com/JacobAsmuth/qed\">https://github.com/JacobAsmuth/qed</a></p>",
        "id": 625220505,
        "sender_full_name": "West",
        "timestamp": 1789732430
    },
    {
        "content": "<p>A concrete place to look at for the DOM model is ProofWidgets4. <code>ProofWidgets.Html</code> (<code>ProofWidgets/Data/Html.lean</code>) already has <code>element</code>, <code>text</code> and <code>component</code> (React components with JSON props), and there's JSX support too, so you can do <code>open scoped ProofWidgets.Jsx</code> and then <code>&lt;div&gt;xyz&lt;/div&gt;</code>. Uppercase tags are components.</p>\n<p>It's used in the VS Code infoview rather than as a standalone web page, but it's probably the most mature HTML type in Lean right now.</p>\n<p>For WASM I can only speak from using hand-written WASM on a website, not from building Lean to WASM. In general the page needs <code>'wasm-unsafe-eval'</code> in the CSP, and I'd use SRI and <code>WebAssembly.instantiateStreaming</code> for loading it. Size is also something to watch out for, since the Lean runtime is already a few mb.</p>\n<p>The usual setup could be something like the WASM module exporting a pure <code>update</code> function and JS handling the <code>view</code>. We've also kept a WASM implementation in sync with a JS reference before, which caught quite a few bugs.</p>\n<p>I haven't tried the Lean-to-WASM build myself though, so I don't know how rough that part is at the moment.</p>",
        "id": 625233714,
        "sender_full_name": "JoelCanary",
        "timestamp": 1789735964
    },
    {
        "content": "<p><span class=\"user-mention\" data-user-id=\"955304\">@West</span> , lean-vir is for now FRO-internal, but it will very likely improved to a more suitable form for external users in Q4 2026.</p>\n<p>lean-vir (and sister project lean-fir) does work by compiling a subset of Lean to WASM. You can manipulate DOM from there using regular WASM to JS bridges. We provide some tooling for this, and an experimental bindings surface where a type <code>Js Bar</code> means \"an JS object of type <code>Bar</code> is living in the JS heap.</p>\n<p>There is not formal model of the DOM in VIR or FIR. <code>lean-fir</code> does interact with the Talos WASM model and targets it.</p>",
        "id": 625246242,
        "sender_full_name": "Emilio Jesús Gallego Arias",
        "timestamp": 1789738945
    },
    {
        "content": "<p>I completely forgot about proofwidgets, so thank you for reminding me <span class=\"user-mention\" data-user-id=\"1203785\">@JoelCanary</span>.</p>\n<p>I'll definitely try out lean-vir <span class=\"user-mention\" data-user-id=\"110154\">@Emilio Jesús Gallego Arias</span>. Good to know it's actively being worked on.</p>",
        "id": 626768502,
        "sender_full_name": "West",
        "timestamp": 1790300217
    }
]