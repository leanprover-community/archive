[
    {
        "content": "<p>I'm trying to set up my lsp so that it depends on <code>elan</code> instead of the <code>lean</code> or <code>lake</code> commands, so I've disabled <code>lean.nvim</code>'s lsp config and manually added my own autocmd with <code>vim.api.nvim_create_autocmd</code> and <code>vim.lsp.start</code>. Issue is, this only configures the lsp and having the infoview reflect the proof state by using what's given by the lsp isn't set up by this. Would anyone have a high-level idea/overview of how the infoview interacts with the lsp so I can configure it in lua?</p>",
        "id": 481237089,
        "sender_full_name": "nrs",
        "timestamp": 1731037303
    },
    {
        "content": "<p>I'm not sure I follow -- <code>elan</code> provides (wraps) those two commands. Specifically <code>lean.nvim</code> is out of the box expecting to be using elan. What issue were you having?</p>",
        "id": 481239828,
        "sender_full_name": "Julian Berman",
        "timestamp": 1731039598
    },
    {
        "content": "<p><span class=\"user-mention silent\" data-user-id=\"321696\">Julian Berman</span> <a href=\"#narrow/channel/113488-general/topic/High-level.20idea.20of.20how.20the.20infoview.20in.20lean.2Envim.20works.3F/near/481239828\">said</a>:</p>\n<blockquote>\n<p>I'm not sure I follow -- <code>elan</code> provides (wraps) those two commands. Specifically <code>lean.nvim</code> is out of the box expecting to be using elan. What issue were you having?</p>\n</blockquote>\n<p>If I check <code>:LspInfo</code>, the command used to start the lsp is <code>lake serve</code>, and for some reason <code>List.IsSuffix</code> is not recognized when importing <code>import Init.Data.List.Basic</code>. This issue disappears when I start the lsp with <code>elan run stable lake serve</code></p>",
        "id": 481243313,
        "sender_full_name": "nrs",
        "timestamp": 1731042257
    },
    {
        "content": "<p>ah, it turns out I was using an outdated version of <code>lean.nvim</code>! thanks for pointing out that it's expected to use <code>elan</code>, with the newest version I now get a call to <code>elan</code></p>",
        "id": 481246280,
        "sender_full_name": "nrs",
        "timestamp": 1731044888
    }
]